let SessionLoad = 1
if &cp | set nocp | endif
let s:so_save = &g:so | let s:siso_save = &g:siso | setg so=0 siso=0 | setl so=-1 siso=-1
let v:this_session=expand("<sfile>:p")
let NetrwMenuPriority =  80 
let NetrwTopLvlMenu = "Netrw."
silent only
silent tabonly
cd ~
if expand('%') == '' && !&modified && line('$') <= 1 && getline(1) == ''
  let s:wipebuf = bufnr('%')
endif
let s:shortmess_save = &shortmess
if &shortmess =~ 'A'
  set shortmess=aoOA
else
  set shortmess=aoO
endif
badd +1 projects/toolbox
badd +18 projects/wikify/wit-tree.c
badd +1 projects/wikify/mywikitest/.wiki/table
badd +1 projects/wikify/mywikitest/.wiki/tree
badd +811 /usr/include/c++/12/bits/stl_list.h
badd +39 /usr/include/glib-2.0/glib/glist.h
badd +12 projects/wikify/makefile
badd +1 projects/wikify/make:\ \*\*\*\ \[makefile
badd +1 projects/wikify
badd +13 projects/toolbox/test.c
badd +95 projects/toolbox/toolbox.h
argglobal
%argdel
$argadd projects/wikify/makefile
$argadd projects/wikify/wit-tree.c
set lines=26 columns=112
edit projects/wikify/wit-tree.c
argglobal
2argu
balt projects/toolbox/toolbox.h
let s:l = 62 - ((8 * winheight(0) + 12) / 24)
if s:l < 1 | let s:l = 1 | endif
keepjumps exe s:l
normal! zt
keepjumps 62
normal! 033|
lcd ~/projects/toolbox
tabnext 1
if exists('s:wipebuf') && len(win_findbuf(s:wipebuf)) == 0
  silent exe 'bwipe ' . s:wipebuf
endif
unlet! s:wipebuf
set winheight=1 winwidth=20
let &shortmess = s:shortmess_save
let s:sx = expand("<sfile>:p:r")."x.vim"
if filereadable(s:sx)
  exe "source " . fnameescape(s:sx)
endif
let &g:so = s:so_save | let &g:siso = s:siso_save
doautoall SessionLoadPost
unlet SessionLoad
" vim: set ft=vim :
